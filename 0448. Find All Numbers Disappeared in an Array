Given an array nums of n integers where nums[i] is in the range [1, n], 
return an array of all the integers in the range [1, n] that do not appear in nums.


SOURCE CODE:

class Solution {
public:
    vector<int> findDisappearedNumbers(vector<int>& nums) {
        vector<int> res;
        map<int,int> mp;
        int s = nums.size();
        for(auto n:nums){
            mp[n]++;
        }
        for(int i=1;i<=s;i++){
            if(mp[i]==0)
            res.push_back(i);
        }
        return res;
    }
};
/*
After mp[n]++ map become like this
mp[1] = 1
mp[2] = 2
mp[3] = 2
mp[4] = 1
mp[7] = 1
mp[8] = 1
*/

OUTPUTS:

Example 1:
Input: nums = [4,3,2,7,8,2,3,1]
Output: [5,6]

Example 2:
Input: nums = [1,1]
Output: [2]
 
